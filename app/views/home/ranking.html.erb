<!DOCTYPE html>
<html>
<head>
  <title>User Relationship Count</title>
</head>
<body class="ranking-body">
  <h1>Ranking</h1>
  <table>
    <tr>
      <th>User ID</th>
      <th>解いた問題数</th>
    </tr>
    <% @users.each do |user| %>
      <tr>
        <% if @current_user.id == user.id %>
          <td style="color: red"><%= user.email %></td>
        <% else %>
          <td style="color: black;"><%= user.email %></td>
        <% end %>
        <td><%= user.relationships.size %></td>
      </tr>
    <% end %>
  </table>
</body>
</html>
